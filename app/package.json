{
  "name": "devops-demo-app",
  "version": "1.0.0",
  "description": "Demo API for CI/CD + Kubernetes + monitoring project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --colors"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
